const fs = require("fs");
const path = require("path");
process.env.NODE_PATH = 'C:\\Dem\\Kvplat\\kvplat2024\\Files\\UT\\node_modules';
require('module').Module._initPaths();
const sql = require("../../SITE_UT/db.js");

const OSBB_PATH = "C:/Users/Dima.DEM/.codex/attachments/f054d3ae-eb6e-4748-b687-0efa3a2119b6/pasted-text.txt";
const COOP_PATH = "C:/Users/Dima.DEM/.codex/attachments/98f76ad5-5a89-464b-9799-8439e130b5a5/pasted-text.txt";

const COURT_PENALTY_TEXT_BLOCK = `{courtPenaltyText
@@both
Відповідно до пункту 3 частини першої статті 161 ЦПК України судовий наказ може бути видано, якщо заявлено вимогу про стягнення заборгованості за оплату житлово-комунальних послуг з урахуванням індексу інфляції та трьох процентів річних, нарахованих заявником на суму заборгованості.

Відповідно до частини другої статті 625 Цивільного кодексу України боржник, який прострочив виконання грошового зобов’язання, на вимогу кредитора зобов’язаний сплатити суму боргу з урахуванням установленого індексу інфляції за весь час прострочення, а також три проценти річних від простроченої суми, якщо інший розмір процентів не встановлений договором або законом.

У зв’язку з простроченням боржником виконання грошових зобов’язань заявником нараховано:

**інфляційні втрати — {сума інфляційних втрат} грн;**

**три проценти річних — {сума 3 % річних} грн.**

Детальний розрахунок інфляційних втрат та трьох процентів річних додається до заяви.
@@inflation
Відповідно до пункту 3 частини першої статті 161 ЦПК України судовий наказ може бути видано, якщо заявлено вимогу про стягнення заборгованості за оплату житлово-комунальних послуг з урахуванням індексу інфляції, нарахованого заявником на суму заборгованості.

Відповідно до частини другої статті 625 Цивільного кодексу України боржник, який прострочив виконання грошового зобов’язання, на вимогу кредитора зобов’язаний сплатити суму боргу з урахуванням установленого індексу інфляції за весь час прострочення.

У зв’язку з простроченням боржником виконання грошових зобов’язань заявником нараховано:

**інфляційні втрати — {сума інфляційних втрат} грн.**

Детальний розрахунок інфляційних втрат додається до заяви.
@@annual3
Відповідно до пункту 3 частини першої статті 161 ЦПК України судовий наказ може бути видано, якщо заявлено вимогу про стягнення заборгованості за оплату житлово-комунальних послуг з урахуванням трьох процентів річних, нарахованих заявником на суму заборгованості.

Відповідно до частини другої статті 625 Цивільного кодексу України боржник, який прострочив виконання грошового зобов’язання, на вимогу кредитора зобов’язаний сплатити три проценти річних від простроченої суми, якщо інший розмір процентів не встановлений договором або законом.

У зв’язку з простроченням боржником виконання грошових зобов’язань заявником нараховано:

**три проценти річних — {сума 3 % річних} грн.**

Детальний розрахунок трьох процентів річних додається до заяви.
@@none
Відповідно до пункту 3 частини першої статті 161 ЦПК України судовий наказ може бути видано, якщо заявлено вимогу про стягнення заборгованості за оплату житлово-комунальних послуг.
}`;

const COURT_PENALTY_APPENDICES_BLOCK = `{courtPenaltyAppendices
@@both
1. Розрахунок інфляційних втрат та трьох процентів річних.
@@inflation
1. Розрахунок інфляційних втрат.
@@annual3
1. Розрахунок трьох процентів річних.
@@none
}`;

const COURT_DEBT_SUMMARY_BLOCK = `{courtDebtSummary
@@housing
* заборгованість зі сплати внесків та платежів на утримання будинку — **{сума заборгованості на утримання будинку} грн**;
@@tpv
* заборгованість зі сплати платежів за вивезення побутових відходів — **{сума заборгованості за тпв} грн**;
@@inflation
* інфляційні втрати — **{сума інфляційних втрат} грн**;
@@annual3
* три проценти річних — **{сума 3 % річних} грн**.
@@total
**Усього до стягнення за основними вимогами: {сума} грн.**
}`;

const COURT_CLAIMS_REQUESTS_BLOCK = `{courtClaimsRequests
@@housing
**Видати судовий наказ про стягнення з {fio} на користь {org}, код ЄДРПОУ {okpo} {текст основної заборгованості} за квартирою загальною площею {pl} кв. м, розташованою за адресою: {flatAddress}, у розмірі {сума заборгованості на утримання будинку} грн.**
@@tpv
**Стягнути з {fio} на користь {org}, код ЄДРПОУ {okpo} заборгованість зі сплати платежів за вивезення побутових відходів за квартирою загальною площею {pl} кв. м, розташованою за адресою: {flatAddress}, у розмірі {сума заборгованості за тпв} грн.**
@@inflation
**Стягнути з {fio} на користь {org}, код ЄДРПОУ {okpo} інфляційні втрати у розмірі {сума інфляційних втрат} грн.**
@@annual3
**Стягнути з {fio} на користь {org}, код ЄДРПОУ {okpo} три проценти річних у розмірі {сума 3 % річних} грн.**
@@courtFee
**Стягнути з {fio} на користь {org}, код ЄДРПОУ {okpo} судовий збір у розмірі {сума судового збору} грн.**
}`;

function readText(filePath) {
  return fs.readFileSync(path.normalize(filePath), "utf8").replace(/\r\n/g, "\n");
}

function normalizeCourtTemplate(text) {
  let out = String(text || "").replace(/\r\n/g, "\n");
  out = out.replace(/\*\*До\s+\[НАЗВА СУДУ\]\*\*/gi, "**До ____________________**");
  out = out
    .replace(/Об’єднання співвласників багатоквартирного будинку «\{org\}»/g, "{org}")
    .replace(/Об’єднанням співвласників багатоквартирного будинку «\{org\}»/g, "{org}")
    .replace(/Об’єднання співвласників багатоквартирного будинку \{org\}/g, "{org}")
    .replace(/Об’єднанням співвласників багатоквартирного будинку \{org\}/g, "{org}")
    .replace(/ОСББ «\{org\}»/g, "{org}")
    .replace(/ОСББ \{org\}/g, "{org}")
    .replace(/Житловий кооператив «\{org\}»/g, "{org}")
    .replace(/Житлового кооперативу «\{org\}»/g, "{org}")
    .replace(/Житловим кооперативом «\{org\}»/g, "{org}")
    .replace(/Житловому кооперативу «\{org\}»/g, "{org}")
    .replace(/Житловий кооператив \{org\}/g, "{org}")
    .replace(/Житлового кооперативу \{org\}/g, "{org}")
    .replace(/Житловим кооперативом \{org\}/g, "{org}")
    .replace(/Житловому кооперативу \{org\}/g, "{org}");
  out = out.replace(/\{adr\},\s*кв\.\s*\{kv\}/g, "{flatAddress}");
  out = out
    .replace(/\[ДАТА ПОЧАТКУ\]/gi, "{дата початку}")
    .replace(/\[ДАТА ЗАКІНЧЕННЯ\]/gi, "{дата закінчення}")
    .replace(/\[ДАТА РОЗРАХУНКУ\]/gi, "{дата розрахунку}")
    .replace(/\[ДАТА\]/gi, "{дата}")
    .replace(/\[СУМА ОСНОВНОЇ ЗАБОРГОВАНОСТІ\]/gi, "{сума основної заборгованості}")
    .replace(/\[СУМА ЗАБОРГОВАНОСТІ ЗА ТПВ\]/gi, "{сума заборгованості за тпв}")
    .replace(/\[СУМА ІНФЛЯЦІЙНИХ ВТРАТ\]/gi, "{сума інфляційних втрат}")
    .replace(/\[СУМА 3 % РІЧНИХ\]/gi, "{сума 3 % річних}")
    .replace(/\[СУМА СУДОВОГО ЗБОРУ\]/gi, "{сума судового збору}")
    .replace(/\[СУМА\]/gi, "{сума}");
  out = out
    .replace(/\[АДРЕСА ОСББ\]/gi, "{адреса осбб}")
    .replace(/\[АДРЕСА КООПЕРАТИВУ\]/gi, "{адреса кооперативу}")
    .replace(/\[ТЕЛЕФОН\]/gi, "{телефон}")
    .replace(/\[E-MAIL\]/gi, "{e-mail}")
    .replace(/\[ПІБ\]/gi, "{головаFull}");
  out = out.replace(
    /^При зверненні до суду[\s\S]*?сплачено судовий збір у розмірі\s*\*\*\{сума судового збору\}\s*грн\*\*,\s*що підтверджується платіжним документом\./gim,
    "Заява подається до суду в електронній формі через підсистему «Електронний суд». З урахуванням коефіцієнта 0,8, передбаченого частиною третьою статті 4 Закону України «Про судовий збір», заявником сплачено судовий збір у розмірі **{сума судового збору} грн**, що підтверджується платіжним документом."
  );
  out = out.replace(
    /відомості про наявність електронного кабінету:\s*\[___\]/gi,
    "відомості про наявність електронного кабінету: наявний"
  );
  out = out
    .replace(/^РНОКПП:\s*\[ЗА НАЯВНОСТІ\]\s*\n?/gim, "")
    .replace(/^дата народження:\s*\[ЗА НАЯВНОСТІ\]\s*\n?/gim, "")
    .replace(/^зареєстроване місце проживання:\s*\[ЗА НАЯВНОСТІ\]\s*\n?/gim, "");
  out = out.replace(
    /Рішенням загальних зборів ОСББ «\{org\}», оформленим протоколом № \*\*\[НОМЕР ПРОТОКОЛУ\]\*\* від \*\*\[ДАТА ПРОТОКОЛУ\]\*\*, затверджено розмір внесків та платежів співвласників\./gi,
    "Розміри внесків та платежів на утримання та експлуатацію багатоквартирного будинку і прибудинкової території затверджені відповідними рішеннями загальних зборів {org}, оформленими протоколами, копії яких додаються до заяви."
  );
  out = out.replace(
    /Рішенням загальних зборів ОСББ «\{org\}», оформленим протоколом № \*\*\[НОМЕР ПРОТОКОЛУ\]\*\* від \*\*\[ДАТА ПРОТОКОЛУ\]\*\*, затверджено розмір платежу співвласників за вивезення побутових відходів\./gi,
    "Розміри платежів за вивезення побутових відходів затверджені відповідними рішеннями загальних зборів {org}, оформленими протоколами, копії яких додаються до заяви."
  );
  out = out.replace(
    /Рішенням загальних зборів Житлового кооперативу «\{org\}», оформленим протоколом № \*\*\[НОМЕР ПРОТОКОЛУ\]\*\* від \*\*\[ДАТА ПРОТОКОЛУ\]\*\*, затверджено розміри внесків та платежів на утримання та експлуатацію багатоквартирного будинку і прибудинкової території\./gi,
    "Розміри внесків та платежів на утримання та експлуатацію багатоквартирного будинку і прибудинкової території затверджені відповідними рішеннями загальних зборів {org}, оформленими протоколами, копії яких додаються до заяви."
  );
  out = out.replace(
    /Розмір платежу за вивезення побутових відходів затверджено рішенням загальних зборів Житлового кооперативу «\{org\}», оформленим протоколом № \*\*\[НОМЕР ПРОТОКОЛУ\]\*\* від \*\*\[ДАТА ПРОТОКОЛУ\]\*\*\./gi,
    "Розміри платежів за вивезення побутових відходів затверджені відповідними рішеннями загальних зборів {org}, оформленими протоколами, копії яких додаються до заяви."
  );
  out = out.replace(
    /\*\s+заборгованість[\s\S]*?\*\*Усього до стягнення за основними вимогами:\s*\[___\]\s*грн\.?\*\*/i,
    COURT_DEBT_SUMMARY_BLOCK
  );
  out = out
    .replace(/\{tozrahunokBorgu\}/g, COURT_DEBT_SUMMARY_BLOCK)
    .replace(/\{rozrahunokBorgu\}/g, COURT_DEBT_SUMMARY_BLOCK);
  out = out.replace(
    /# ПРОШУ СУД:\s*[\s\S]*?(?=\n## Додатки:)/i,
    `# ПРОШУ СУД:\n\n${COURT_CLAIMS_REQUESTS_BLOCK}\n\n`
  );
  out = out.replace(/\{courtClaimsRequests\}/g, COURT_CLAIMS_REQUESTS_BLOCK);
  out = out
    .replace(/статтями\s+509,\s*525,\s*526,\s*625\s+Цивільного кодексу України/gi, "статтями {courtCivilCodeArticles} Цивільного кодексу України")
    .replace(/статтями\s+509,\s*525,\s*526\s+Цивільного кодексу України/gi, "статтями {courtCivilCodeArticles} Цивільного кодексу України");
  out = out.replace(
    /Відповідно до пункту 3 частини першої статті 161 ЦПК України[\s\S]*?Детальний розрахунок інфляційних втрат та трьох процентів річних додається до заяви\./gi,
    COURT_PENALTY_TEXT_BLOCK
  );
  out = out.replace(/\{courtPenaltyText\}/g, COURT_PENALTY_TEXT_BLOCK);
  out = out.replace(
    /^\d+[\.\)]\s*Розрахунок інфляційних втрат\.\s*\n\d+[\.\)]\s*Розрахунок трьох процентів річних\.\s*$/gim,
    COURT_PENALTY_APPENDICES_BLOCK
  );
  out = out
    .replace(/^\d+[\.\)]\s*Розрахунок інфляційних втрат та трьох процентів річних\.\s*$/gim, COURT_PENALTY_APPENDICES_BLOCK)
    .replace(/^\d+[\.\)]\s*Розрахунок інфляційних втрат\.\s*$/gim, COURT_PENALTY_APPENDICES_BLOCK)
    .replace(/^\d+[\.\)]\s*Розрахунок трьох процентів річних\.\s*$/gim, COURT_PENALTY_APPENDICES_BLOCK);
  out = out.replace(/\{courtPenaltyAppendices\}/g, COURT_PENALTY_APPENDICES_BLOCK);
  out = out.replace(
    new RegExp(`${COURT_PENALTY_APPENDICES_BLOCK.replace(/[.*+?^${}()|[\]\\]/g, "\\$&")}\\s*\\n\\s*${COURT_PENALTY_APPENDICES_BLOCK.replace(/[.*+?^${}()|[\]\\]/g, "\\$&")}`, "g"),
    COURT_PENALTY_APPENDICES_BLOCK
  );
  out = out.replace(
    /^\d+[\.\)]\s*Копія протоколу[^\n]*(загальних зборів|протоколу|протоколів)[^\n]*(внесків|платежів|вивезення побутових|ТПВ)[^\n]*$/gim,
    "1. Копії протоколів (витяги з протоколів) загальних зборів Кооперативу, якими встановлено розміри внесків та платежів, що діяли у відповідних розрахункових періодах."
  );
  out = out.replace(
    /^\d+[\.\)]\s*Копії протоколів[^\n]*(загальних зборів|протоколу|протоколів)[^\n]*(внесків|платежів|вивезення побутових|ТПВ)[^\n]*$/gim,
    "1. Копії протоколів (витяги з протоколів) загальних зборів Кооперативу, якими встановлено розміри внесків та платежів, що діяли у відповідних розрахункових періодах."
  );
  out = out.replace(
    /^\s*(1\.\s+Копії протоколів[^\n]+)\s*\n\s*\1\s*$/gim,
    "$1"
  );
  out = out.replace(/\[[^\]]+\]/g, "");
  return out.replace(/\n{3,}/g, "\n\n").trim();
}

function orgKind(value) {
  const text = String(value || "").trim().toUpperCase();
  if (text.startsWith("ОСББ")) return "osbb";
  if (text.startsWith("ЖБК") || text.startsWith("ЖК")) return "coop";
  return "coop";
}

function splitTpvSection(text) {
  const source = String(text || "").trim();
  const startRe = /^###\s+Заборгованість за вивезення побутових відходів\s*$/im;
  const startMatch = startRe.exec(source);
  if (!startMatch) return { main: source, tpv: "" };
  const before = source.slice(0, startMatch.index).trimEnd();
  const restStart = startMatch.index;
  const rest = source.slice(restStart);
  const nextRe = /\n###\s+Правові підстави[^\n]*\n/i;
  const nextMatch = nextRe.exec(rest);
  if (!nextMatch) return { main: `${before}\n\n{ТПВ}`.trim(), tpv: rest.trim() };
  const tpv = rest.slice(0, nextMatch.index).trim();
  const after = rest.slice(nextMatch.index).trimStart();
  return {
    main: `${before}\n\n{ТПВ}\n\n${after}`.trim(),
    tpv
  };
}

async function upsertTemplate(homeCode, legacyCode, docNumber, summary, body) {
  await sql`
    insert into public.outgoing_documents (
      home_code,
      doc_date,
      doc_number,
      recipient,
      summary,
      body,
      signature_text,
      account_id,
      is_draft,
      is_template,
      legacy_code
    )
    values (
      ${String(homeCode)},
      current_date,
      ${docNumber},
      '',
      ${summary},
      ${body},
      '',
      '',
      false,
      true,
      ${legacyCode}
    )
    on conflict (legacy_code) do update set
      home_code = excluded.home_code,
      doc_date = excluded.doc_date,
      doc_number = excluded.doc_number,
      recipient = excluded.recipient,
      summary = excluded.summary,
      body = excluded.body,
      signature_text = excluded.signature_text,
      account_id = excluded.account_id,
      is_draft = false,
      is_template = true
  `;
}

async function main() {
  const osbb = splitTpvSection(normalizeCourtTemplate(readText(OSBB_PATH)));
  const coop = splitTpvSection(normalizeCourtTemplate(readText(COOP_PATH)));
  const homes = await sql`
    select code, name, data->>'org' as org
    from public.homes
    order by code
  `;
  let saved = 0;
  for (const home of homes) {
    const kind = orgKind(home.org || home.name);
    const tpl = kind === "osbb" ? osbb : coop;
    const kindLabel = kind === "osbb" ? "ОСББ" : "ЖК";
    await upsertTemplate(
      home.code,
      `${home.code}:court_claim_${kind}_main`,
      `ИсковоеЗаявление${kindLabel}`,
      `ИсковоеЗаявление${kindLabel}`,
      tpl.main
    );
    saved += 1;
    if (tpl.tpv) {
      await upsertTemplate(
        home.code,
        `${home.code}:court_claim_${kind}_tpv`,
        `ИсковоеЗаявление${kindLabel}_ТПВ`,
        `ИсковоеЗаявление${kindLabel}_ТПВ`,
        tpl.tpv
      );
      saved += 1;
    }
  }
  console.log(`court claim templates saved: ${saved}`);
}

main()
  .catch(err => {
    console.error(err);
    process.exitCode = 1;
  })
  .finally(() => sql.end());
