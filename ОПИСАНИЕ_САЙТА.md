# Учет в ОСББ/ЖК

## Назначение

Приложение предназначено для бухгалтерского и абонентского учета по домам ОСББ/ЖК.
Это клиентское веб-приложение на `HTML/CSS/Vanilla JS` с авторизацией через Supabase
и доступом к данным и документам конкретных домов.

Рядом с ним находятся:

- `index.html` — основное приложение.
- `info.html` — отдельная промо-страница.
- `files/` — файловый архив по домам, годам и месяцам.

## Бизнес-правила

- Основная сущность учета: `billing_accounts`.
- Базовое правило распределения оплат: FIFO.
- Назначение платежа не считается авторитетным источником распределения.
- Для начислений, льгот по начислениям и ручных корректировок используются `posting_month` и `billing_month`.
- Для оплат используются `payment_date` и `payment_month`.
- Для распределений оплат используются `billing_month_from` и `billing_month_to`.
- Месяц хранится как целое число: `year * 12 + month`.

## Технологии

- `HTML`
- `CSS`
- `JavaScript` без сборщика
- `Supabase Auth`
- `Supabase PostgREST`
- `PDF.js`, `JSZip`, `ExcelJS`, `html2canvas`, `FileSaver`, `EmailJS`

## Структура проекта

### Входные страницы

- `index.html` — основной интерфейс приложения.
- `info.html` — отдельный лендинг по услугам бухгалтера.

### JavaScript

- `js/menu.js` — загрузка домов, построение меню, переходы по действиям, запуск приложения.
- `js/table.js` — таблицы по лицевым счетам, начислениям, оплатам, фильтрации и сортировке.
- `js/paytable.js` — платежи за выбранный период.
- `js/banktable.js` — банковские операции и фильтры.
- `js/reports.js` — просмотр и скачивание PDF/Excel/изображений.
- `js/generated-reports.js` — генератор зведених звітів: реєстр платежів, реєстри особових рахунків, списки боржників/переплатників, оголошення про борги, PDF/Excel/друк.
- `js/outgoing-documents.js` — генератор вихідних документів і листів з HTML-переглядом, Word-експортом, PDF/друком та діями біля сторінок.
- `js/meeting-protocols.js` — генератор протоколів зборів, списків реєстрації, листів голосування та опитування.
- `js/gr-common.js` — спільні утиліти генераторів: листи `gr-sheet`, дії сторінок, PDF через `html2canvas`, друк, placeholders, rich text.
- `js/homeinfo.js` — реквизиты дома и подстановка значений в шаблоны документов.
- `js/schema.js` — визуальная схема дома.
- `js/analiz.js` — аналитические сводки.
- `js/dashboard.js` — дашборд.
- `js/akt_sverki.js` — акт сверки.
- `js/ut.js` — общие утилиты.
- `js/addstuf.js` — вспомогательная логика карточки лицевого счета и дополнительных действий.
- `js/supabase-client.js` — инициализация клиента Supabase.
- `js/app-auth.js` — bootstrap авторизации и входа в приложение.

### CSS

- `css/allLs.css` — основные стили приложения.
- `css/menu.css` — боковое меню.
- `css/banktable.css` — банк.
- `css/reports.css` — просмотр документов.
- `css/print.css` — печать.
- `css/analiz.css` — аналитика.
- `css/schema.css` — схема дома.
- `css/dashboard.css` — дашборд.

## Основные сценарии

### Авторизация

- Вход и регистрация идут через Supabase Auth.
- При открытии `index.html` приложение сначала проверяет сессию.
- Если сессия активна, пользователь сразу переводится в режим приложения.
- Если сессии нет, показывается форма входа и регистрации.

### Работа с домами

- Пользователь видит только дома, к которым у него есть связь через `user_homes`.
- После загрузки домов строится левое меню действий.
- Если в URL есть `homeCode` и `actionCode`, открывается соответствующий раздел.
- Иначе открывается первый доступный дом и первое действие.

### Основные разделы интерфейса

- лицевые счета
- перечень и табличные режимы
- платежи
- банк
- документы
- информация о доме
- схема дома
- аналитика
- дашборд
- акт сверки

### Генераторы документов

- Раздел `Документи` открывает файловую/документную панель через `reportsInit(homeCode)`.
- `globalReports` использует `renderGlobalReports()` и может формировать несколько типов отчетов по одному или нескольким домам.
- Для прямого показа одного отчета есть `window.renderGeneratedReportOnly({ reportId/typeId/report, homeCode, from, to, month, compact })`.
- В отчетах страницы имеют класс `gr-sheet`; кнопки копирования/поделиться возле страниц работают через `html2canvas`.
- PDF отчетов, писем и протоколов формируется как изображения страниц внутри PDF.
- `outgoingDocuments` открывает генератор исходящих документов через `openOutgoingDocuments(homeCode)`.
- `meetingProtocols` открывает генератор протоколов через `openMeetingProtocols(homeCode)`.

## Данные и доступ

### Таблицы, которые реально использует клиент

По текущему коду клиент напрямую работает как минимум с:

- `homes`
- `user_homes`
- `corrections`

### Что хранится в `homes`

Из клиентского кода видно, что из таблицы читается поле `data`, внутри которого лежит JSON
с описанием дома, действиями, файлами и справочными данными. После чтения этот JSON
парсится на клиенте.

### Файловый архив

Папка `files/` организована по иерархии:

- дом
- год
- месяц
- тип документа или подпапка

Эта структура используется модулем просмотра документов.

## Проверка RLS

RLS был проверен по реальным метаданным базы через соседний проект
`C:\Dem\Kvplat\kvplat2024\Files\OUT\AllLS\SITE_UT`, где есть `service_role`
и прямое подключение к Postgres.

### Таблицы `public` с включенным RLS

- `corrections`
- `homes`
- `user_homes`

### Фактические политики

#### `homes`

- `SELECT`: пользователь видит дом, если существует строка в `user_homes`,
  где `user_homes.home_code = homes.code` и `user_homes.user_id = auth.uid()`.
- `INSERT`: разрешен одному конкретному admin user.
- `UPDATE`: разрешен одному конкретному admin user.

#### `user_homes`

- `SELECT`: только строки, где `user_id = auth.uid()`.

#### `corrections`

- есть permissive policy для `public` на `SELECT`
- есть permissive policy для `public` на `INSERT`
- есть permissive policy для `public` на `UPDATE`
- есть permissive policy для `public` на `DELETE`
- есть permissive policy для `public` на `ALL`

Практический результат проверки через `anon`-ключ:

- `homes` без авторизации возвращает пустой набор
- `user_homes` без авторизации возвращает пустой набор
- `corrections` без авторизации читается успешно

По решению владельца проекта `corrections` пока остается открытой,
поскольку в этой таблице нет секретных или критичных данных.

## Supabase admin-доступ

Секреты не хранятся в этом репозитории и не должны копироваться в документацию.
Для административных задач используется соседняя локальная утилита:

`C:\Dem\Kvplat\kvplat2024\Files\OUT\AllLS\SITE_UT`

В ней точно есть service-role/admin-доступ в `server.js`: там создается
`supabaseAdmin = createClient(...)`, который используется для admin API и операций
с таблицами через Supabase JS.

Также в `SITE_UT/db.js` есть прямой Postgres-helper. Именно через него проверялись
права на `CREATE`, поэтому для DDL/миграций удобнее подключаться через этот модуль,
не перенося строку подключения в файлы сайта.

Проверка от 08.08.2026 показала:

- `current_user`: `postgres`
- `current_database`: `postgres`
- `CREATE` в базе: есть
- `CREATE` в схеме `public`: есть
- `USAGE` в схеме `public`: есть

Безопасный read-only запрос для повторной проверки прав:

```sql
select
  current_user,
  current_database(),
  has_database_privilege(current_user, current_database(), 'CREATE') as can_create_db,
  has_schema_privilege(current_user, 'public', 'CREATE') as can_create_public,
  has_schema_privilege(current_user, 'public', 'USAGE') as can_use_public;
```

Важно: временные скрипты с прямым подключением не коммитить. Если нужен постоянный
скрипт миграции, он должен импортировать `SITE_UT/db.js` или использовать подход из
`SITE_UT/server.js`, но не содержать секретов внутри себя.

## Архитектурные изменения

- `index.html` больше не содержит inline-инициализацию Supabase и auth-bootstrap.
- Инициализация клиента вынесена в `js/supabase-client.js`.
- Проверка сессии, вход, регистрация и переключение режимов вынесены в `js/app-auth.js`.
- В `js/menu.js` убран повторный автозапуск `initApp()`, чтобы приложение не инициализировалось дважды.

## Legacy-очистка

- Из `js/ut.js` убран Babel/regenerator helper-пролог.
- В `js/ut.js` и `js/addstuf.js` заменены `concat`-цепочки на шаблонные строки.
- Логика работы не менялась, правки ограничены структурой кода и текстовыми строками интерфейса.

## Технические риски

- Проект работает без bundler и без модульной системы.
- Значительная часть логики находится в глобальной области видимости.
- Есть сильная зависимость от CDN-скриптов и глобальных переменных.
- В проекте остаются крупные legacy-файлы, которые стоит чистить дальше только небольшими шагами.

## Ближайшие безопасные шаги

1. Дальше выносить inline-логику из больших файлов только маленькими шагами.
2. Продолжить чистку `addstuf.js`, `table.js` и соседних модулей без изменения бизнес-правил.
3. Постепенно разделять доступ к данным, преобразование данных и DOM-рендеринг.
